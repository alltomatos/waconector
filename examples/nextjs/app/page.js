export default function Home() {
  return (
    <main>
      <h1>waconector — exemplo Next.js</h1>
      <p>
        Veja <code>app/api/webhook/route.js</code> para o webhook mínimo (usa{' '}
        <code>MockAdapter</code> por padrão). Instruções completas no <code>README.md</code> deste
        diretório.
      </p>
    </main>
  );
}
